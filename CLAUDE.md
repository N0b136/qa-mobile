# Questland Adventures — workspace guide

Pitch-ready mobile PWA for the Questland adventure park. App code lives in `questland/`.

## Stack
React 19 + TypeScript + Vite + vite-plugin-pwa. react-router v7. No CSS framework — DS tokens mirrored to `questland/src/theme/ds/*.css` (dark-first stone + gold-as-metal, from `design-system/`); `theme/tokens.css` is a BRIDGE keeping old var names (--parchment/--ink/--gold/--forest/--arcane/--ember + -soft) pointed at DS values. Fonts via @fontsource (bundled, offline PWA), icons via lucide-react. Local-first service layer (`questland/src/services/`, localStorage-backed) mirrors Firestore collections so Firebase can swap in later; components never touch localStorage directly. All lore/content is data in `questland/src/content/*.ts` — placeholder lore until user material lands in `content-intake/`.

## Commands
- Dev server: `npm run dev` in `questland/` (PWA enabled in dev)
- Build: `npm run build` · Icons: `npm run icons`
- Node installed at machine level; refresh PATH in PowerShell sessions if `node` not found.

## Dev team (subagents in .claude/agents)
- `architect` — design before code; returns build spec only
- `frontend-dev` — React/TS implementation (main workhorse)
- `pwa-engineer` — service worker, manifest, offline, notifications
- `content-writer` — lore/copy edits in src/content (haiku; use for ALL pure-content work)
- `qa-tester` — drives flows in browser at 375x812, reports defects
- `code-reviewer` — post-feature diff review, findings only

## Token efficiency (house rules)
**Fable/Opus orchestrates only — it never writes code.** All implementation (ts/tsx/css/html/configs) is delegated to subagents. Route pure-content edits to `content-writer` (haiku). Read line ranges, not whole files. Edit > rewrite. One agent per concern; don't chain agents when inline work is cheaper. Terse agent replies: files changed + one-line notes. `/caveman` skill available when terse output wanted. Keep this file under 200 lines — summarize, don't grow.

## Design system (CANON — mirrored in design-system/, source DesignSync id fd4a099b-4d30-456b-92fa-c7d33afaf0dd)
Read `design-system/readme.md` before any UI work. Non-negotiables: **dark-first** stone ground (parchment = documents only: tickets/maps/scrolls, via Card tone="parchment"); gold = metal, primary actions only (never flat fills, never on parchment); ruby/sapphire = narrative track colors only; type Cinzel (display, ALL CAPS tracked, <6 words) / EB Garamond (body) / Alegreya Sans (UI, only face <14px) / IM Fell English (scribe, max 2 lines); radii 2/4/6/10 + signature top **arch**; hairline gold borders; carve-in shadows; motion mechanical (160ms standard, 720ms door-ease reveals, fade+8-12px rise, no bounce/spring); hover lightens, press darkens+down; locked = dashed hairline + kept arch shape, never 40% opacity; focus ring 2px gold. **NO EMOJI ANYWHERE** — Lucide icons (lucide-react) per readme's glyph vocabulary; brand marks are raster assets, never redrawn. Naming: ticket=Passage, group=Party, reward=Seal, achievement=Badge, stop=Station, route=Questline, staff=Guide/Warden. Voice: warm ceremonious guild, second person, concrete numbers, no "immersive/unforgettable", ≤1 exclamation per page. DS components (jsx+d.ts) in design-system/components; app screen proposals in design-system/ui_kits/app (TrailMap, StationCheckIn, Collection, Party) — adapt, don't ignore. UI brief (pre-DS, superseded where they conflict): `design/claude-design-brief.md`.

## Canon (user-provided, in content-intake/)
Orgs: **Rangers of Questia** (ranks Scout ep5, Watcher ep9), **Hearers of the Alehiim** (Temblers ep5, the Hidden ep9), **Order of the Elm** (Sworn of the Elm ep5, Guardian of the Elm ep9); induction at ep1. One 10-episode questline each + 21 rotating stations (7/org/episode; types puzzle/craft/song/riddle/obstacle/story/base) + season finale "RAID: Brigand's Return" (3 phases). Ticketing (4 categories): day Adventurer $25/Hero $35 (u3 free); group $90/$125 up to 6 +$5/person; monthly "Citizen of Questia" membership $30/$35/$45 +$5/member w/ Hero member events; birthday $350/$450. Normalized: content-intake/season-1.json + ticketing.json. Park map (FINAL, user-remade 2026-07-25): `content-intake/park-map.png` (2752x1536, 11MB — optimize to webp before bundling). Every station prints number+name so **map marker N = station st-N** (verified vs season-1.json), plus village amenities (Entry Village, Adventurer's Hall, Forest Tavern, Gravel Parking Lot, Entrance). Known gap: station **15 Maker's Cave omitted** by the generator — place as an app pin. Naming canon: park = **Whispering Woods Adventure Park**; region/world = **Questia** ("the Wilds of Questia"); central hub = **Village of Queston** (official, user-set 2026-07-25, replaces "Questia Village"/"Village of Questin"). Old `park-map-sample.svg` superseded. **Org→track colors (user-confirmed 2026-07-25):** Rangers of Questia=Valor(ruby/red), Hearers of the Alehiim=Wilds(green), Order of the Elm=Lore(sapphire/blue); Forge/ember reserved for live/now states, gold = primary actions only. **Org themes (onboarding quiz, user-given 2026-07-25):** Rangers=Nature/Courage/Survival, Hearers=Hope/Faith/Healing, Elm=Integrity/Adventure/Intrigue. **Org brand glyphs (user-supplied rasters → Seal `art`):** Rangers=compass-sword bronze medallion (`art="compass"`), Hearers=pomegranate-tree stone relief over river (`art="relief"`), Elm=crowned-tree gold pin (`art="crown"`). App brand rasters live in `questland/public/assets/` (served at `/assets/`; Seal `assetBase="/"`): badge-compass-sword.png, relief-pomegranate-tree-stone.png, badge-crowned-tree-gold.png, logo-questland-primary.png (gemstone-Q lockup = TopBar/Welcome brand mark).

## Status (2026-07-26)
Done: scaffold, services, full-canon content layer, shell + Welcome/Auth/Onboarding/Home/Notifications/More screens (pre-DS look). DESIGN SYSTEM IMPORTED: 45 text files mirrored to `design-system/` (readme.md authoritative); theme rebridged **dark-first** (`src/theme/ds/` tokens + @fontsource fonts + lucide-react; tokens.css bridge; `npm run build` clean). DS COMPONENT LIBRARY COMPLETE: all 20 DS components ported to importable TS in `questland/src/ui/` (barrel `index.ts`; Icon reimplemented on lucide-react; org `track` field wired to confirmed colors). SCREENS: chrome (TopBar w/ real logo, BottomNav→TabBar) + HomeScreen DS-adopted, build clean, **user-approved direction 2026-07-25** (verified via computed styles — stone ground, Cinzel, org rank Badges render exact confirmed track colors, emoji-free). User mandate: "use the Design System for the entire thing." ALL APP SCREENS DS-ADOPTED (build clean): Welcome (real logo + Button nav), Auth (Card form + DS Input), Notifications (type→glyph, Card rows, unread Badge dot), More (Card menu, "My party" rename, danger sign-out). ONBOARDING = **org-selection flow** (avatar/sigil picker SCRAPPED per user 2026-07-25), rebuilt as a `step` machine in `screens/OnboardingScreen.tsx`: name → **gate ("Pick a Quest" = manual vs "Help me decide" = quiz)** → manual all-3 picker OR 6-question quiz (auto-advance, back nav) → best-fit result (tie-safe, never auto-picks; "show all three" override). Stores `user.orgId` (starting order; all 3 questlines still open later) via widened `updateProfile`; Home profile mark now renders chosen order's brand **Seal** (art per org). Quiz content in `content/orgQuiz.ts` (haiku-authored: 6 Qs, one option/org each, all 9 themes ×2, `ORG_THEMES`/`ORG_QUIZ` exports). User-confirmed canon: "Tremblers" spelling; ep1 induction titles (Ranger Recruit / Hearer / Member of the Order). QUESTS & PROGRESSION DONE + browser-verified (Phase 3): `/quests` (QuestsScreen — all 3 orgs, your order first), `/quests/:orgId` (QuestlineScreen — Seal/rank header, featured current-episode ArchCard, vertical ProgressTrail over 10 eps w/ rank checkpoints at 1/5/9, completed-ep lore Dialog), `/quests/:orgId/check-in` (CheckInScreen — parchment quest-brief, 7-station list w/ type-glyphs, staff-code + camera-QR paths). Progression engine (progressService, pre-existing) wired: staff code `RANG-101` verified end-to-end → progress written, rank-up (Seeker→Ranger Recruit) + lore notifications fire EMOJI-FREE (purged: sigil/📜/⬆️ → Lucide icon fields). QrScanner (html5-qrcode@2.3.8) camera path built w/ graceful no-camera degradation — NOT verifiable in build env (no camera), staff-code path is. Fixed DS bug: Dialog corner ✕ now wired to onClose. BOOKING DONE + browser-verified (Phase 5): bookingService (priceBooking pure calc for all 4 categories, createBooking→`ql:bookings:${uid}` + booking notification, listBookings, cancelBooking); BookScreen `/book` (5-step: passage→details→addons→review→done, all 8 tiers/4 categories, date+slot+party steppers, add-on switches, live totals, SIMULATED pay, confirmation code); BookingsScreen `/bookings` (My Passages list, Dialog-gated cancel). Smoke-tested end-to-end: Hero Pass ×2 adults + Feast add-on → total $88 correct, booking persisted w/ code, notification emoji-free (`ticket`), cancel flips status. bookingTiers.ts emoji purged → Lucide icon names. PARTY LINKING DONE + browser-verified (Phase 8): partyService (`ql:parties` store — createParty w/ 6-char code, joinParty case-insensitive + notifies existing members, leaveParty w/ empty-party cleanup, getUserParty); PartyScreen `/party` (no-party create/join cards; in-party header w/ invite code + copy, member roster w/ per-member level + 3-org progress bars, YOU badge, Dialog-gated leave). Verified: create→code, 2nd user joins by lowercase code (case-insensitive), roster + progress correct, join notification emoji-free, leave returns to no-party. LOCKED-EPISODE BLUR (user req 2026-07-25): ProgressTrail gained opt-in `blurLocked` prop (filter blur(4px)+userSelect none+aria 'Locked episode' on locked labels); QuestlineScreen passes it — locked episode titles unreadable until unlocked (complete/current stay legible). LEADERBOARD DONE + browser-verified (user req 2026-07-25): `leaderboardService` (pure — `playerStandings()`/`partyStandings()`, standard competition ranking; players ranked by totalXp→completed→name, parties by summed member XP; `completed` = episodes across all 3 orgs, max 30; added `listUsers`/`listParties` service exports); `/leaderboard` (LeaderboardScreen — Parties|Adventurers toggle, "Your standing/party — #N of M" line, ranked Cards w/ top-3 gold rank marks, own-row highlight + YOU/YOURS Badge, no-party→"Join a Party" prompt); Home "Standings" widget (tappable→/leaderboard, your-party-rank line + top-5 parties); entry links from More (first menu item), Quests header, Party screen. Verified w/ 5 seeded users/3 parties: party board #1 Ashen Vanguard YOURS 22 eps, adventurer board Alder YOU #1 18/30, solo user→join prompt, all emoji-free. HELP & SOS DONE + browser-verified (Phase 7, user req 2026-07-25): `sosService` (global `ql:sos` list so the staff/demo console can read all; `createSos`/`acknowledgeSos`/`resolveSos`/`activeSosFor`/`listSos`/`listUserSos`; SosRequest types pre-existed in types.ts); `/help` (HelpScreen replaces placeholder — two tiers: **Emergency** "Summon a Warden" w/ zone Select + optional message + danger SEND SOS + always-on `tel:+15550100` call-line anchor + live open→acknowledged status + Stand Down/resolve; **Quest-help** "Ask a Guide for a hint" w/ open→ack status + Never Mind). NO staff console yet, so HelpScreen has a DEMO `useEffect` that auto-acknowledges an open request after 2.5s (Warden Aldous / Guide Wren) — the real demo console (task #11) will drive this later. Zone glyphs via local ZONE_ICON Lucide map (zones.ts `icon` field is still emoji — ignored, not rendered). Entry: existing More "Help & SOS" + new "Need aid?" quick-link on CheckInScreen. Verified: emergency SOS→open→"Warden Aldous is on the way" (zone+message shown)→stand down restores form; quest-help→"Guide Wren is bringing a hint"; all notifications type 'sos' icon shield/life-buoy EMOJI-FREE; tel href correct. DEMO CONSOLE DONE + browser-verified (Phase 9, task #11): `demoService` (`seedDemoWorld`/`resetDemoData`, all seeded users+parties use `demo-` id prefix); `/demo` (DemoConsoleScreen, reached via TopBar's pre-existing 7-taps-on-logo gesture) = presenter's remote w/ 3 panels: **Demo data** (Seed world → 4-member cast + 2 parties incl. host's Ashen Vanguard + host mid-season progress 6/3/1 + Hero booking + notifications + one open emergency SOS; Reset → Dialog-gated, clears all demo-/sos/host-progress/bookings/notifications but KEEPS host account+session), **Fire a notification** (Event/Booking/Lore/System/SOS-broadcast, in-world copy, Lucide icons), **Live calls for aid** (lists non-resolved SOS w/ guest+kind+zone+message; Dispatch=acknowledge as Warden Aldous/Guide Wren, Resolve). HelpScreen's simulated auto-ack REMOVED — the console now drives SOS acknowledgment (guest /help sits at "Alerting the Wardens…" until dispatched). Verified: seed populated all stores, dispatch flipped SOS→acknowledged + notified guest, fire-lore grew host inbox, reset returned to solo host. ZONES REBASED on canon (user req 2026-07-25): `zones.ts` now DERIVES `ZONES` from the 21 canon `STATIONS` (stations.ts) + a **Village of Queston** hub (id 'village'), Lucide `icon` per station type (emoji purged); org `homeZoneId`→base-station ids (st-19/20/21). SOS picker verified showing the Village of Queston + all 21 station names, emoji-free. VILLAGE NAMING RESOLVED (user-set 2026-07-25): central hub officially **Village of Queston** — renamed everywhere in content (zones.ts hub name+blurb, 6 bookingTiers perks, BookScreen "Back to Queston"), build clean. `pois.ts`/`quests.ts`/`events.ts`/`loreNotes.ts` still carry OLD placeholder zoneIds (gatehouse/hollow/etc.) — unrendered now; **remap to station/village ids at map-build time (#6)**. NOTIFICATIONS WIRED + browser-verified (Phase 6, task #8): the in-app center (NotificationsScreen — type→glyph, relative time, mark-all-read, permission opt-in CTA, unread dot) + TopBar bell unread badge were ALREADY built; this wave added the missing automatic triggers: **booking reminders** (`bookingService.syncBookingReminders(uid)` — fires "Your visit is today/tomorrow" once per booking per threshold, deduped via `ql:reminders:${uid}`, local-date; run from a `Shell` useEffect keyed only on user.id to avoid tick loops), **tap-to-navigate** (NotificationsScreen taps now markRead + navigate by type: booking→/bookings, lore→/quests, sos→/help, system→/party, event→/), and a **welcome notification** fired at ONBOARDING completion (OnboardingScreen.join, real name + chosen order: "{name}, the {org} welcome you…", moved OUT of authService.signUp where the name was only a provisional email-prefix). Verified: reminders fired for today+tomorrow only (far/cancelled skipped), dedup held across Shell re-mount, tap routed to /bookings + marked read, welcome read "Nedra Vale, the Rangers of Questia welcome you." Real FCM web push deferred to production (needs Firebase); `notificationService.add` already best-effort fires a browser Notification via the SW when permission granted. MAP DONE + coord-verified (task #6, 2026-07-25): `/map` MapScreen — pan/zoom `public/assets/park-map.webp` (2400x1340, ~1MB, from user's final `content-intake/park-map.png`); 21 station pins from `content/stationMap.ts` (STATION_COORDS 0..1 fractions, verified via `scripts/verify-map.mjs` dot-composite) incl. **st-15 Maker's Cave = synthetic app pin** SW of the mine (generator omitted it) + 5 amenity POIs (MAP_LANDMARKS: Village of Queston/Adventurer's Hall/Forest Tavern/Gravel Parking Lot/Entrance). Pins state-styled by user's active org: active=track-glow current-episode stations (via `stationsFor(currentEpisode)`), visited=gold completed-eps, home=base badge (st-19/20/21), else neutral hairline. Tap station→Dialog w/ "Check in here" (base stations override to their own org's check-in); amenity→info Dialog. "You are here" marker at DEFAULT_POSITION, reads `ql:demo:position` (demo hook). `questIcons.STATION_NOTE` added (type→blurb). PWA POLISH DONE + QA-verified (task #12, 2026-07-25): app icons regenerated from user's finished `content-intake/icon-rock-bg.png` master (192/512 any + safe-zone-padded maskable 192/512 + apple-touch 180 + gem-Q favicons via `scripts/generate-icons.mjs`, repeatable `npm run icons`); manifest (id '/', 4 icons w/ purpose, DS stone theme/bg) + iOS meta (apple-touch, favicons, black-translucent status bar) in index.html; workbox `globPatterns` gained webp/jpg/jpeg/ico → `park-map.webp` + 70 woff2 fonts confirmed in `dist/sw.js` precache (offline-ready); install flow `hooks/useInstallPrompt.ts` + MoreScreen "Install Questland" row (Android beforeinstallprompt / iOS A2HS Dialog). FULL QA REGRESSION (375x812, DOM-based) — all flows clean (rename verified: zero "Questia Village" left; emoji audit clean); fixed 2 findings: `demoService.seedDemoWorld` now removes host from any pre-existing real party before seeding (no double-party membership), `MapScreen` "you are here" marker got role=img+aria-label. **ALL 15 BUILD TASKS COMPLETE — app is pitch-ready.** Remaining is pre-production only (deferred): real FCM web-push backend (needs Firebase); dead/unrendered content-data emoji (orgs.ts `sigil`, events/pois/loreNotes icon fields) — purge if/when those get wired.
**BACK OFFICE CONSOLE + FIRESTORE COMM LANES DONE + live cross-device verified (2026-07-26):** desktop staff console (`/console`) that talks to the guest phone app over Firebase Firestore (real-time notifications + SOS dispatch). **Transport:** Firestore (modular v12, anonymous auth), web config COMMITTED BY DESIGN in `services/firebaseConfig.ts` (public — security is via rules only; real project `qa-mobile-36a9c`; do NOT flag the committed apiKey as a leak). `firebase.ts` = lazy code-split bootstrap (`ensureFirebase()`, `CloudState` machine disabled/connecting/live/offline); ALL firestore ops via dynamic `import('firebase/*')` so the ~573kB chunk never loads under placeholder config — `isConfigured()` false ⇒ ZERO network, app byte-identical to local-only, console shows "Local only" and still works cross-tab. **Scope = comm lanes only:** SOS, notifications, guest directory → Firestore; accounts/progress/bookings/parties stay local-first, screens untouched. **The one seam:** `cloudSync.ts` (the whole bridge) `onSnapshot` → `store.save()` into local mirrors (`ql:sos`/`ql:sosMeta`/`ql:notifications:${uid}`/`ql:guestDirectory`/`ql:scheduled`) → every untouched screen lights up via existing `useAppTick`. Merges: SOS by-id LWW on `updatedAt` + status-rank tiebreak (open<ack<resolved, skew can't resurrect); notifications read-monotonic + cap-50; `deepEqual` guard against notify churn; `removed` docChanges handled. Write-throughs appended to `sosService`/`notificationService`/`authService`/`progressService` (signatures UNCHANGED, fire-and-forget). Live-listener banners: phone's added-notification snapshot → existing SW `showNotification` (gated `!hasPendingWrites` & `createdAt>syncStart` & not-pre-merge-local & addressee-only) — exactly one banner, only on the addressee's device (also fixed a latent quirk where party-join fan-out banner'd the author). Echo/loop structurally impossible: merges only ever `store.save()`, never push. cloudSync does NOT statically import notificationService (lazy `import()` in `banner()` breaks the cycle). **Console** (`src/screens/console/*`): standalone route OUTSIDE Shell, no RequireAuth; persona gate (`content/staff.ts` — Warden Aldous/Maera, Guide Wren/Bram; persona key `ql:staffPersona` is LOCAL only, never synced). Panels: **CallsBoard** (open/ack SOS lanes, guest+zone via `ql:sosMeta`, Dispatch→acknowledgeSos(persona), Resolve), **SendWord** (Tabs Compose|Scheduled; audience all/guest/party/org from live directory; 8 templates `content/consoleTemplates.ts`; Now/Later datetime-local), **GuestRoster** (live directory rows, org track Badge, per-row send prefill). `consoleService.ts` = thin layer over cloudSync+store. **Console-as-scheduler** (no server compute by decision): `scheduled` collection + 15s ConsoleScreen ticker → `fireDueSchedules()` `runTransaction` claim-and-fire (double-fire-safe across tabs); overdue schedules fire on next console open. `demoService.seedDemoWorld` pushes cast guest profiles + a sample "Lantern Rite" schedule; `resetDemoData` deletes demo comm docs (chunked writeBatch). `/demo` UNTOUCHED. **Live-verified:** two-context round-trip through real Firestore — roster sync, Send Word delivery, SOS open→dispatch→resolve, scheduled fire, SW OS-banner — ALL PASS, 0 config errors (anon auth + rules correct). Code review: 1 Medium fixed (`pushGuestProfile` `setDoc merge:true` stranded stale `partyId`/`partyName` after leave-party in the console roster + party broadcasts → dropped merge, buildGuestDoc is the complete/only writer, commit 7efa87b); rest LOW/demo-grade accepted. **⚠ SECURITY CAVEAT (demo-grade, explicit user decision — NOT production-safe):** Firestore rules are open to ANY authenticated (anonymous) client, so anyone with the public config can read/write the 3 comm collections. Deferred to production: FCM + Cloud Functions (push-when-closed + server-side scheduling — currently scheduled sends need an OPEN console tab at fire time); tightened rules. Optional-not-shipped: `experimentalAutoDetectLongPolling` (QA found no genuine WebChannel stall; the test-env long-polling need was a headless-Chromium-vs-proxy TLS issue, irrelevant to a real phone).
**STATION CARD ART DONE (user-supplied 2026-07-26):** 21 station photos landed as `questland/public/assets/stations/st-01..21.png` (70.8 MB of masters). MapScreen requests `assets/stations/${station.id}.webp` (ids ARE zero-padded `st-01`..`st-21`), so masters are converted by `scripts/optimize-stations.mjs` (`npm run stations`, sharp → 840px-wide q80 webp): **70.8 MB → 2.09 MB**, ~100 KB/file, all 21 in the sw.js precache. **PNG masters are gitignored** (`questland/public/assets/stations/*.png`) — user decision 2026-07-26, they stay on the user's machine only; re-run `npm run stations` to regenerate the tracked webp. `vite.config.ts` workbox gained `globIgnores: ['**/assets/stations/*.png']` so the masters (present on local disk, absent on CI) can't blow the 4 MB precache ceiling. Same master→webp pattern as park-map. Pushed as commit d7892f0.
**GATE INTRO DONE (design handoff `design_handoff_gate_intro/README.md`, implemented 2026-07-26):** cold-open gate video that freezes into the app backdrop. Asset `questland/public/assets/video-gate-opening.mp4` (2.66 MB, 1280x720, 10.005s — the user's `Downloads/Starting_from_this_picture_a.mp4`, the exact file the handoff prototype referenced; **do NOT re-encode**, runtime does the work: `playbackRate = duration/5` = 2.001 and `object-fit:cover` portrait-crops it into the 480px column). New `components/GateIntro.tsx` = fixed `<video>` (z0) + scrim (z1, literal rgba stops per spec) + input shield (z70 → `visibility:hidden` at 5s); on `ended` it pauses at `duration - 0.05` and **stays mounted forever as the fixed backdrop** — never unmount it, never swap in a poster. **TRIGGER (user-set 2026-07-26, after two revisions — this is the final rule): the intro plays the FIRST TIME HOME LOADS PER SESSION, however the guest got there** — cold open on a restored session AND straight after account creation + order picking (`/onboarding` sits outside Shell, so finishing it mounts Shell fresh at `/`). GateIntro captures `useLocation().pathname` ONCE in a `useState` initializer: `skip = pathname !== '/' ⋁ hasSeenGate() ⋁ prefers-reduced-motion`. The `pathname !== '/'` guard covers a reload/deep-link on `/map`, `/quests` etc. — the video freezes as the backdrop instead of blocking a sub-screen for 5s, and deliberately does NOT `markGateSeen()`, so a genuine Home load later in the same session still plays. `services/gateIntroService.ts` = `hasSeenGate`/`markGateSeen` only (sessionStorage `ql:gate-seen`, set only when the intro actually plays). Rejected earlier designs, do not reintroduce: a boot-time `armGateIntro(!!currentUser())` in main.tsx (blocked the post-onboarding play the user wants). Timing lives in CSS custom props set from JS (`--intro-ui-delay` 4.55s / `--intro-scrim-delay` 4.2s / `--intro-shield-dur` 5s) — **set in a `useLayoutEffect`, and the `var(x, fallback)` fallbacks must equal the intro values**; a post-paint `useEffect` or `0s` fallbacks make the whole UI flash in at t=0 and snap back (bug found + fixed in review). Shell wraps TopBar / Outlet / BottomNav in **three separate** reveal wrappers — only the Outlet wrapper may carry the `qa-ui-rise` translateY; a transform on an ancestor of the fixed TopBar/BottomNav re-parents them and breaks the layout, so those get opacity-only `qa-fade-in`. **TRANSFORM GOTCHA (bug the user hit post-merge, fixed in PR #5 / commit b744c1b — the map went blank):** `qa-ui-rise` originally rose via `transform: translateY`, but a transformed element is the CONTAINING BLOCK for `position: fixed` descendants (and `fill: both` keeps it applied forever), so `MapScreen`'s fixed root resolved top/bottom against the near-zero-height Outlet wrapper and collapsed. The keyframe now rises via **`top: 10px → 0`** on the already-`position: relative` wrapper — `top` never establishes a containing block, at any point in the animation. **Never put transform/filter/perspective/will-change on the Outlet wrapper**; screens are free to use `position: fixed` (MapScreen does, twice). **STACKING GOTCHA (bug the user hit, fixed):** an animated `opacity` with `fill:both` makes each wrapper a permanent stacking context, so TopBar/BottomNav's own `z-index:50` resolves INSIDE its wrapper — a static wrapper is then ~0, i.e. BELOW the Outlet wrapper's 10, and page content paints over the fixed bars. Both bar wrappers therefore carry `position:relative; zIndex:50` (relative is safe — it does NOT re-parent fixed children; only transform/filter/perspective/contain/will-change do). Final chain: video 0 < scrim 1 < Outlet 10 < bars 50 < shield 70 (intro only) < toast 90. Keyframes `qa-fade-in`/`qa-ui-rise`/`qa-scrim-in`/`qa-shield-off` in `theme/global.css`. TopBar bg → `--surface-overlay` + `backdrop-filter: var(--blur-veil)` (DS sticky-header-over-photography treatment). HomeScreen untouched. `vite.config.ts` globPatterns gained `mp4` so the backdrop survives offline. DOM-verified: vars/delays/easing (`--ease-out-door`) exact, rate 2.001, **measured runtime 5.077s**, freeze lands on `duration-0.05`, all 3 trigger cases (new account→onboarding→Home plays; cold open on `/` plays; reload on `/map` freezes w/ vars 0s + flag untouched, and the next `/` load in that same session still plays), TopBar computed bg `rgba(18,18,20,.82)` + `blur(14px)`. User previewed it on the dev server and approved ("looks great"). **Shipped as PR #4, merged to main 2026-07-26** (commit 43d3623) after merging in the InstallBanner work that landed on main meanwhile (PRs #2/#3). **InstallBanner ↔ GateIntro:** the banner renders at the App root, OUTSIDE Shell/GateIntro, so it isn't in the `qa-fade-in` reveal group and used to pop in opaque over the playing gate; it now carries the same `qa-fade-in` keyed to `var(--intro-ui-delay, 0s)` — 0s fallback, so it reveals immediately everywhere except a genuine intro. Correct by construction, don't "fix": banner z55 < shield z70 (input stays blocked during the intro), and `--install-banner-height` offsets only TopBar/screen padding, never GateIntro's `top:0/height:100dvh` layer, so the backdrop can't misalign. TopBar padding uses main's renamed `--chrome-safe-top` (banner-aware), not `--safe-top`.
**PASSAGE-GATED QUEST STARTS DONE (user req 2026-07-28, build clean + browser-verified):** a quest cannot be taken without a ticket. **The one choke point is the chief's house** — `checkIn(uid, QUEST_START.id)` now spends a Quest Experience off a passage; everything downstream (the episode's 7 station taps) is free, because the walk is what the ticket already bought. Gate arrival (`checkInAtGate`) stays free by design: you can stand in the Village of Queston without a quest. **Rules are DATA, off the tickets themselves** — `BookingTier.pass: PassRule {validFor, questsPerGuest, per}` in `content/types.ts`, one per tier in `bookingTiers.ts` (Adventurer 1/guest/day · Hero unlimited/day · group tiers the same terms · Adventurer Membership 1/guest/**week** for a month · Hero+Family Hero unlimited/month · birthday packages 1/guest on the day). Allowance = `questsPerGuest × (adults+children)`; weekly/monthly periods run from the booking's own start date, not the calendar. **`services/passService.ts`** is the whole rule engine + ledger (`ql:passUses:${uid}`, local-only, not synced — a passage is spent on the device that presents it): `passStates`/`validPasses` (status valid|upcoming|expired|spent|cancelled + `remaining`/`note`), `redeem`, `recordCover`, `useForEpisode`, `clearUsesFor`. **One episode = one Quest Experience**, so `redeem` is idempotent per (uid, orgId, episodeId) — re-calling on the chief mid-walk costs nothing (verified). **`checkIn` return type CHANGED** to `CheckInResult` = `({ok:true} & CheckInOutcome) | {ok:false, error, needPass?}`; `checkInAtGate` too. Screens try WITHOUT a passage first — an episode already paid for passes straight through, `needPass` opens `components/PassagePicker.tsx` (Dialog: valid passages tappable w/ remaining Badge, invalid ones listed dim with the reason, "Book a passage" → /book). Wired at all three quest starts: QuestlineScreen, CheckInScreen, MapScreen's chief pin. **Parties:** one passage walks in as many as its booked headcount (`PassUse.covers`); the tapper is always covered, others take remaining seats in member order, and anyone past the count is NOT refused — they simply present their own passage from their own phone. `Presence` gained `passCode`/`passName`/`passCovers[]` (synced), so `syncParty` on a mate's phone only records a cover when that guest is in `passCovers`, and the console's GuestsAfield shows the passage a party walked in on. BookingsScreen shows "Quest Experiences — N of M left"/"Unlimited" + the note. `demoService` now seeds a **Hero Pass for TODAY (3 adults, matches the Vanguard)** plus an upcoming Adventurer Pass, gives the seeded cast presence rows a passCode, and `resetDemoData` wipes the ledger. Browser-verified at 375x812: no-passage refusal (quest NOT started), Adventurer 1 of 1 → 0 left → second questline refused as SPENT, Hero unlimited across two orders, re-taking the same quest charged nothing, stations free after paying, party of 3 on one Hero Pass (only the tapper charged, mate got a cover), upcoming pass reads "Good from … — 3 days yet", 0 console errors. Not verifiable in sandbox: the Firestore round-trip (known proxy issue).

**STATION RECORDS + CONSOLE PANEL SWAP DONE (user req 2026-07-28, build clean + browser-verified):** the console keeps the day's log. **Panel order swapped** per user: `console-grid` is now **Send Word (left, 1.1fr) | Calls for Aid (middle, 1fr) | Guests Afield (320px)**; The Park still spans the row above, Station Records spans the row below. **`services/questLogService.ts`** is the new append-only log (`ql:questLog`, cap 2000, newest-first) — presence is LWW and overwrites itself, so nothing there survives a walk. `QuestLeg` (in `types.ts`, beside `Presence`) = one CHECK-IN EVENT: id `${groupId}:${placeId}:${at}`, group (party or solo-as-party-of-one) + memberNames + partySize, place, org/episode, `legNumber` (0 = chief's house, 1..7 = station ordinal, **unset for a gate arrival**), `sealed`, passage, and `runId` = `${groupId}|${orgId}|${episodeId}`. **One leg per event, written by the device that tapped** — `presenceService.logLeg` fires from `checkIn`/`checkInAtGate`; `syncParty` deliberately does NOT log (a party is one row, not one per member). Dedupe: same run + same place → skipped (re-taking a quest or re-tapping a walked station costs nothing); arrivals dedupe on a 30-min window. **Runs are DERIVED, not stored** (`questRuns()`), so a late-syncing leg lands in the right walk: legs grouped by runId, the group's latest gate arrival within 6h before the first leg joined on as leg 0. An arrival that opened no walk gets its own "No quest taken" row — unless it falls inside a run the group was already on, which would list the party twice. **`screens/console/StationRecords.tsx`** = full-width panel: day Select (Today / each earlier day / All), counts (walks · groups · check-ins · sealed), table (Party · Quest · Arrived · Quest taken · Stations n/7 + Sealed badge · Last leg · Elapsed) expanding to per-leg lines with clock time, place and `+ N min` since the previous leg. **Export CSV** = one row per LEG (20 columns incl. `minutes_since_previous`/`minutes_since_start`/`run_id`), UTF-8 BOM so Excel opens it right, filename `questland-station-records-<day>.csv` — long format on purpose: it appends onto a master sheet day after day and pivots. **Cloud:** new `legs` collection — console-only subscription (`orderBy('at','desc') limit 2000`), `pushLeg` write-through, merge is a plain union by id. **`firestore.rules` UPDATED, user must re-publish**: create = own uid (+ `demo-`), read/delete = staff or own, **update = never** (a leg is written once). `demoService` seeds a full day (Lantern Circle sealed el-03 then 3 stations into el-04, Vanguard arrived + quest taken, Quill just through the gate) using the CANON rotations — which also **corrected the seeded presence** for Sable/Thorn from st-06 to st-08 Songhollow Cave (next ElmRoot), since st-06 is not on the el-04 rotation; `resetDemoData` calls `clearLegsFor`. Verified in-browser: gate→chief→2 stations logged with right legNumbers/passage/party, dedupe held at 19 legs across a re-take + re-tap, 4 runs derived correctly (incl. the 7/7 sealed one at 78 min), console geometry measured (Send Word x=24 w=536, Calls x=584, Guests x=1096), expand + real CSV download (17 rows, BOM), 0 console errors. NOT verifiable in sandbox: the Firestore round-trip, and a REAL staff sign-in (no credentials here) — the signed-in console was rendered by temporarily bypassing `revalidateStaff`, reverted after.

**Brand rasters DONE:** logo-questland-primary.png (transparent) + 3 org Seal rasters saved to `questland/public/assets/` + optimized + wired (TopBar logo, org Seals). **User directive 2026-07-25: in-app chrome (TopBar/Welcome) uses the transparent `logo-questland-primary.png` ONLY — do NOT use/await any `logo-questland-on-stone.png` fallback plate.** **PWA/home-screen APP ICON (user-supplied 2026-07-25): `content-intake/icon-rock-bg.png` = FINISHED square app icon (full Questland logo + banner already composited on dark slate/rock). Use AS MASTER — no compositing. Generate icon sizes (192/512, apple-touch 180, favicon) from it; for the `maskable` purpose generate a SAFE-ZONE-PADDED variant (artwork ~78-80% centered on a dark-slate fill sampled from its corners) so the bottom banner + outer filigree ring don't clip under Android circular masks. Handled in PWA polish (#12).** OPTIONAL (non-blocking, not yet provided): four `img-*.png` park photos — `img-gate-closed.png`/`img-gate-threshold.png` (gate hero) + `img-village-path.png`/`img-village-square.png` (village) — only destination is the Welcome gate hero (stone-ground placeholder until/unless supplied).

**VALIDATED NON-LOCAL USERS + PARTIES DONE (user req 2026-07-27, build clean + browser-regressed):** accounts and parties are now server-validated instead of localStorage-only. **Auth = real Firebase Auth email/password** (`cloudAuth.ts` — thin wrapper returning a discriminated result, never throwing); `User.id` IS the Firebase uid; private profile in `accounts/{uid}` (owner-only, the ONLY place an email is stored), public half stays in the existing email-free `guests/{uid}`. `firebase.ts` reworked: exposes `{db, auth}`, keeps an anonymous session as fallback (console + signed-out phones), adds `ensureFirebaseWithin(ms)` — a 10s deadline on every path with a guest watching a spinner, because a blocked network left the SDK's own retries pending **31s** (measured) before falling back. **Two failure kinds drive everything:** `rejected` (server adjudicated — surface it) vs `unavailable` (couldn't ask — fall back to the pre-existing local-only account, byte-identical old behaviour). So the app still runs standalone when the provider is off or the wifi dies. Legacy local accounts self-upgrade: a matching local passHash mints the real Firebase account and `adoptLocalData` migrates progress/bookings/notifications/reminders/party membership onto the new uid. `reconcileSession()` (App effect) drops a cloud session Firebase no longer vouches for — conservative: never touches local-only accounts, never treats an unreachable cloud as signed-out. Password minimum 4→**6** (Firebase's floor). **Parties** are transactional: `parties/{id}` + `partyCodes/{CODE}` claimed via `runTransaction` so two phones can't keep one code, and a join is an O(1) code-doc lookup validated server-side. create/join/leave are now **async** (PartyScreen has busy labels); fixed a latent bug where switching parties left you in the old roster. **Sync scope = account + party + progress + bookings** (user's call): `progress/{uid}` merges as a **UNION** both ways (episodes are monotonic, so no completion can ever be lost), bookings LWW on a new `updatedAt`, `guests` mirrors into `ql:users` as `remote: true` shells so rosters/leaderboard name guests from other devices. `demoService.seedDemoWorld` is now async (awaits the party detach so its local write can't land after the seed). **`firestore.rules` at repo root is NEW and MUST be pasted into the Firebase console** — genuinely enforced now: accounts owner-only, bookings owner-only, progress/guests public-read + owner-write, party updates that can only add/remove YOURSELF; comm lanes (sos/notifications/scheduled) stay open because the console signs in anonymously. **⚠ TWO THINGS THE USER MUST DO for any of the cloud half to activate:** (1) enable **Email/Password** in Firebase console > Authentication > Sign-in method — verified DISABLED (`PASSWORD_LOGIN_DISABLED`) as of 2026-07-27, which is why the app currently takes the local fallback; (2) publish `firestore.rules`. **NOT live-verified:** the cloud path itself — provider is off and this sandbox can't reach Firestore (ERR_CONNECTION_RESET, the known headless-proxy TLS issue). Verified locally instead: signup/duplicate-email/short-password/wrong-password rejections, party create+roster, leaderboard standing, staff-code progression 6→7, demo seed, 0 console errors.

**STAFF ACCOUNTS + COMM-LANE LOCKDOWN DONE (user req 2026-07-27, build clean + live rules-verified):** the console is no longer open to any anonymous client. **Staff = a Firestore allowlist**: a `staff/{uid}` doc, provisioned BY HAND in the Firebase console (`allow write: if false` — nobody can self-promote), is the entire authorisation model; rules gate every staff power on `isStaff()` = `exists(/staff/$(request.auth.uid))`. `/console` now opens on **StaffGate** (real Firebase email/password sign-in; PersonaGate + `ql:staffPersona` DELETED) → `consoleService.signInStaff` → `fetchStaff` → a non-staff account is signed straight back out with "not on the guild roll"; `revalidateStaff()` re-checks on open (awaits the bootstrap first — checking `hasRealAuth()` early would sign out valid staff). Identity comes from the staff doc (`name`/`role`, optional `personaId` borrows a `content/staff.ts` glyph+blurb via new `personaFromStaff`); header "Switch" → "Sign out". **Rules tightened:** sos = own-or-staff (create must be `userId == auth.uid`), notifications = read own only / create own-or-staff, scheduled = staff only. **Live-verified against the real project by REST** (`accounts:signUp` + Firestore REST with real ID tokens, throwaway accounts deleted after): 26/26 on the guest rules, and the pre-lockdown run PROVED the hole — a signed-in guest could read the whole calls board, forge an SOS as another user, write into another guest's inbox and schedule a broadcast to everyone. **Two app changes the lockdown forced:** (1) party-join no longer notifies other members from the joiner's device (that was a cross-guest inbox write) — `cloudSync.announceNewMembers` now raises it on the RECIPIENT's device off the parties-snapshot diff, guarded by a `partiesPrimed` flag so the first snapshot doesn't announce everyone; (2) `demoService` no longer seeds a sample scheduled send (scheduling is staff-only and /demo runs as the presenting guest) — queue one from the console's Compose>Later tab. `deleteDemoCommDocs` rescoped to queries a guest may actually run (own sos/notifications + `demo==true`), each collection in its own try so one denial can't strand the rest. Bounded `demo-` exception in rules (`guests`, `sos`) so the presenter's cast can still be staged — `demo-` ids can't collide with a uid; drop for production. Setup doc: **`staff-accounts.md`** at repo root. Rules PUBLISHED by user 2026-07-27 and re-verified live: **37/37** (26/26 guest + 11/11 staff-deny). Merged to main as PR #6, deployed to Pages.
**LISTENER AUTH-RACE FIX (2026-07-27, found from user's live report — console showed "offline", SOS never reached the board):** the lockdown broke both sync entry points because they subscribed **before** anyone signed in. `ConsoleScreen` called `startConsoleSync()` in a mount effect, so the listeners attached under the ANONYMOUS bootstrap session; `sos` and `scheduled` are refused for anonymous (REST-proved: 403/403, while `guests`/`progress` still returned 200 — which is why the roster looked healthy and the board did not). **Firestore never retries a listener it refused**, so signing in afterwards could not revive them. Fix = `cloudSync.subscribeWhenAuthed(attach)`: wraps both `startGuestSync`/`startConsoleSync`, attaches listeners only once `onAuthStateChanged` reports a REAL (non-anonymous) user, re-attaches on session change, and reports `offline` while there is nothing it may read. `ConsoleScreen` now starts sync + the `fireDueSchedules` ticker in a second effect keyed on `staff?.uid`. **CONSOLE INSTALLS AS ITS OWN APP (user req 2026-07-27):** `/console.html` is a SECOND Vite entry (`build.rollupOptions.input`) with its own document, own manifest (`public/console.webmanifest`) and own entry module (`src/console-main.tsx` — no Router; console screens use zero react-router). Install identity comes from the manifest the DOCUMENT links, so two installable apps need two documents — a route inside the guest app can never install as a separate icon. Manifest uses **all-relative URLs** (`start_url:"console.html"`, `scope:"./"`, `icons:"icons/console-*.png"`) so one static file is correct under both `/` (dev) and `/qa-mobile/` (Pages) — verified resolving to `/qa-mobile/console.html` + `/qa-mobile/`. **VitePWA gotcha:** it injects the GUEST manifest link into EVERY html entry, appending its tags after every `transformIndexHtml` handler AND after `generateBundle` — so the duplicate can only be stripped in `closeBundle` (`consoleManifest()` plugin in vite.config; ours is injected first and would win per spec's first-link-wins rule, but the strip makes it explicit). Icons: `scripts/generate-console-icons.mjs` (`npm run icons:console`) — gold Lucide shield on slate sampled from the same `icon-rock-bg.png` corners so both icons read as one family; PLACEHOLDER-grade, does NOT redraw the brand lockup (DS rule), swap in real art by replacing the script. `/console` route kept for old bookmarks → `ConsoleDoor` `location.replace`s to `console.html` (otherwise "Install" inside the guest app installs the GUEST app under a Back Office name). Verified in-browser at both bases: gate renders, exactly 1 manifest link each, SW does not hand back index.html for console.html, guest app untouched, 0 errors; `/console`→`/console.html` confirmed against a GitHub-Pages-fallback simulation (vite preview resolves extensionless paths itself and hides the redirect — don't test it there). **Standalone-mount gotcha (black screen on reload, fixed same day):** `SendWord` calls `useToast()`, which THROWS without a provider — App.tsx wraps the whole route tree in `ToastProvider`, so the console inherited one for free and the bare `createRoot(<ConsoleScreen/>)` did not. Signed OUT it never showed (StaffGate returns before the panels mount), so the first check passed; signed IN the panels mounted and blanked the page. console-main.tsx now wraps in ToastProvider. **Test the SIGNED-IN console (seed `ql:staffSession`), not just the gate.** ToastProvider is the only provider App.tsx supplies that the console needs — no Router (nothing under `screens/console/` or `ui/` uses react-router), and InstallBanner is guest-only. Known/accepted: both apps share origin storage, so a device signed into the console holds a staff Firebase session the guest app would not expect — keep staff and guest devices separate (documented in `staff-accounts.md`).
Consequence by design: a local-only fallback account subscribes to NOTHING (its id is not a uid, so every owner-scoped query would be refused) — and `pushGuestProfile` is likewise denied for it, so **a guest missing from the console roster means that phone is on a local-only account, not a sync bug**.

**STATION CHECK-IN + CONSOLE STATIONS BOARD DONE (user req 2026-07-27, build clean + browser-verified):** episodes are now sealed by WALKING them. `progressService` gained a station layer (`ql:stations:${uid}` = episodeId→stationIds, `getStationMap`/`stationsDone`/`creditStation`/`creditOrgFor`); the 7th station of the current episode's rotation calls the pre-existing `completeEpisode`, so ranks/XP/lore/notifications are untouched. `completeEpisode` gained `opts.notify` (false only for a party member carried along on THIS device — their own phone announces it) and now backfills the episode's whole station set, so the Guide's staff code + QR paths stay valid as an override. **Check-in = tap the station** (user's call; no per-station QR/codes): MapScreen station card and CheckInScreen rows both call `presenceService.checkIn(uid, stationId, {orgId?})` — the check-in screen passes the ROUTE's org so you can walk another order's questline; a base station always overrules to its own order. **`presenceService.ts` is the new layer:** one `Presence` row per guest in `ql:presence` (LWW on `at`), `STATION_WINDOW_MS` 15min ⇒ at-station, then **en route** (no precise tracking, by design), `PRESENCE_MAX_MS` 4h ⇒ off the board; the check-in that SEALS an episode is flagged `final` on the presence row and **checks the guest out when its 15min expire** (no en-route tail — they finished the walk, user req 2026-07-27), via `isTracked()`; `occupants()` groups rows by partyId (solo guest = party of one) for the console. **Parties move as one:** a check-in writes local presence + progress for every member, but pushes ONLY the tapper's own doc — cross-guest writes are refused by the rules. Each member's phone adopts it off the presence snapshot in `syncParty()` (re-uses the ORIGINAL timestamp, which is what stops two phones volleying) and writes its own row. The record carries `partyMemberNames` so the console can name a whole party from one doc. **Cloud:** new `presence` collection (read: any signed-in; **write: own uid only** + the `demo-` exception) — `firestore.rules` UPDATED, **user must re-publish**. Station progress rides inside the existing `progress/{uid}` doc as a `stations` field, union-merged both ways like `map`. cloudSync subscribes both guest and console to the whole collection; `mergePresenceSnapshot` lazily imports presenceService (cycle guard, same as `banner()`). **Console:** `screens/console/StationsBoard.tsx` — full-width panel above the existing three, the same park chart with occupancy pins (gold `map-pinned` + head-count badge), tap → Dialog naming the parties there with minutes left, plus an "On the paths" en-route list. **`components/MapCanvas.tsx`** = the pan/zoom chart extracted from MapScreen (router-free so the console can use it) with a new `fit` prop: guest keeps `cover`, console uses `contain` (a desktop panel is wider than the chart and `cover` hid half the park). MapScreen otherwise unchanged — its fixed outer wrapper and the transform/stacking gotchas above still apply. `demoService` seeds Lantern Circle at Brigand's Hideout + Ashen Vanguard 22min out (reads as en route) and clears presence on reset. Verified in-browser: 7 map check-ins sealed rg-01 (progress + "Aldric's Key — complete" + "Rank up — Ranger Recruit", emoji-free), route-org credit on the list screen, party fan-out to all 3 Vanguard members, console showed both parties with rosters + minutes left, aged rows flipped to en route and a 6h-old row vanished, 0 console errors. NOT live-verified: the Firestore path (sandbox can't reach it — the known ERR_CONNECTION_RESET).

**ARRIVAL → QUEST → TRAIL PIVOT (user req 2026-07-27, build clean + browser-verified):** the check-in package now carries the whole WALK, and the console is organised around who is in the park rather than who has an account. **Presence record = place + walk:** `kind` ('village' | 'start' | 'station'), `placeName`, plus `orgId/orgName/episodeId/episodeNumber/episodeTitle/stationsDone/stationsTotal/nextStationId/nextStationName` — computed per member AFTER crediting (`walkFor()`), so the console and the guest's own party read where they are, where they are going next, and how far through the episode they are from ONE doc, without touching anyone else's progress. **Three-stage arrival:** `checkInAtGate()` (map → Entrance pin → "We have arrived") puts the party in the **Village of Queston** with NO window — they stay until they take a quest; the **chief's house** (`QUEST_START` in `content/stationMap.ts`, id `st-chief`, synthetic pin at 0.845/0.175 — NOT one of the canon 21, earns no seal, not on any rotation) is the quest start, `START_WINDOW_MS` **5 min** then en route; episode stations keep 15 min. `windowFor()` picks the window by kind; `statusOf()` returns 'village' | 'at-station' | 'en-route'. **Console reorganised:** `GuestRoster` (whole account directory) DELETED → `GuestsAfield.tsx` = parties/solo guests **in the park**, each with status line (In the Village / At X / En route — left X N min ago), quest·episode·N of 7, "Next: <station>", roster, and a send-word prefill. The all-accounts list is SHELVED by user decision — plan a separate console tab later (it grows unbounded). `StationsBoard` retitled "The Park": `PlacePin` (was StationPin) now charts 23 places (21 stations + village + chief's house), dialog names the groups there WITH quest/episode/next, "On the paths" en-route list under the chart. `SendWord` still uses `listDirectory()` for audience resolution — that stayed. **PartyScreen** member rows gained `MemberWhereabouts` (same package, guest-side). **Grouping fix:** `occupants()` prefers the guest's CURRENT party (`getUserParty`) over the partyId frozen into the record, else a check-in made before joining stranded them on the board as a lone traveller. Demo seed rewritten: Lantern Circle at a station mid-episode, Ashen Vanguard en route from the chief's house, new solo cast member **Quill Amberly** (`demo-quill`) just through the gate in the village. Verified in-browser: gate→village→chief's→first station all recorded the right package (0 of 7 → next Abbey Hill Ruins → 1 of 7 → next Hillward Outpost), console showed 3 groups/6 travellers with correct status+quest lines, 23 pins with head-count badges, party screen showed each member's whereabouts, 0 errors.

**ANONYMOUS AUTH REMOVED + PRESENCE RULES LIVE-VERIFIED (2026-07-27):** a REST suite against the real project (`scratchpad/rules-presence.mjs` pattern — throwaway email/password accounts, Firestore REST with real ID tokens, accounts deleted after) found ONE hole in the first published presence rules: an **anonymous** session could write its own `presence` row, because `isSelf()` was satisfied by an anon uid and anybody can mint one from the public web config. Fixed on both sides: (1) rules gained **`isRealUser()`** (`request.auth.token.firebase.sign_in_provider != 'anonymous'`) and EVERY owner predicate — `isSelf`/`ownsExisting`/`ownsIncoming`/`demoIncoming`/`demoExisting` — plus partyCodes/party writes and presence READ now require it; (2) the app **no longer mints an anonymous session at all** — `signInAnonymously` is gone from `firebase.ts` (bootstrap resolves once `onAuthStateChanged` reports, session or not) and from `cloudAuth.cloudSignOut`. The anonymous fallback was vestigial: it predated staff accounts, when the console itself signed in anonymously. User then also **disabled the Anonymous provider** in Firebase console (REST `accounts:signUp` for anon now returns `ADMIN_ONLY_OPERATION`) — so it cannot be minted at all. Rules re-published + re-verified live: **9/9** (own-row write, re-check-in, board read, party-mate read, demo- staging; and refused: forging another guest's row 403, deleting another's row 403, signed-out read 403, anon session unmintable). Side effect worth knowing: a forged `ql:staffSession` in localStorage with no Firebase session behind it is now CLEARED on console open (previously the bootstrap hung on an unreachable network and the stale cache was honoured) — so QA of the signed-in console needs a real staff sign-in, not a seeded key. STILL NOT VERIFIABLE IN SANDBOX: the Firestore **SDK** round-trip — node's fetch reaches googleapis (that is how the REST suite runs), but headless Chromium gets ERR_CONNECTION_RESET through the agent proxy, so the app falls back to a local-only account. Email/Password IS enabled now (REST signUp succeeds), so the cloud path should be live on real devices — needs a two-device check on the user's own machine.

## Skills (.claude/skills — 36 total)
code-review (mattpocock) · karpathy-guidelines · caveman family (7) · webapp-testing · web-design-guidelines · hyperframes suite (25: HTML-to-video for promo/pitch videos — start with `/hyperframes` router). `/simplify` is built into Claude Code — no install needed. Some hyperframes skills carry scanner risk flags; review before first use.
