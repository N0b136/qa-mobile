{
  "name": "questland-functions",
  "private": true,
  "description": "Push sender for Calls for Aid threads, and the Questland Radio ingest pipeline.",
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "radio:test": "npm run build && node test/encode.test.mjs && node test/loads.test.mjs"
  },
  "dependencies": {
    "@ffmpeg-installer/ffmpeg": "^1.1.0",
    "ffprobe-static": "^3.1.0",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@types/ffprobe-static": "^2.0.3",
    "typescript": "^5.8.3"
  }
}
